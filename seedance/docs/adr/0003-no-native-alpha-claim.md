# ADR 0003: No native-alpha claim

Status: accepted

The pipeline uses an explicit matte when transparency is required and verifies keyed edges later.
It does not represent an ordinary generated MP4 as native transparent video.

