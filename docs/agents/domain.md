# Domain docs

This repository uses a single domain context.

Before changing behavior, read:

- `CONTEXT.md` for canonical project language
- applicable records in `docs/adr/` for durable architectural decisions

Use the glossary's preferred terms in tests, code, and project documents. Surface a conflict with an ADR rather than silently overriding it.
