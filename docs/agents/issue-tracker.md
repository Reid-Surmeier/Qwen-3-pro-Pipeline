# Issue tracker: Local Markdown

Issues and PRDs for this repository live as Markdown files in `.scratch/`.

## Conventions

- One feature per directory: `.scratch/<feature-slug>/`
- The PRD is `.scratch/<feature-slug>/PRD.md`
- Implementation issues are `.scratch/<feature-slug>/issues/<NN>-<slug>.md`
- Triage state is a `Status:` line near the top of an issue file
- Comments append under a `## Comments` heading

When a skill says to publish to the issue tracker, create a file under the relevant feature directory. When it says to fetch a ticket, read the referenced local file.
