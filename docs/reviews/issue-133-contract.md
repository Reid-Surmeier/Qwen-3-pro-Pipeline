# Issue 133 Party review contract

Candidate review must verify SHA-256 `f4844fa9030b31b233f43244290f729db105f7256e0c0a6e889f0889bb88366f` and its image-79 crop at `[1107, 505, 215, 269]`. The Party Window presents the five exact source members and meters, starts in Party mode, switches reversibly between Party and Friends, and permits one selected member at a time. Leave is the only source-supported committing action. Info, Memo, Search, and Target must show real pressed feedback and publish a named `TransactionRejectedError` without mutating Party state.

Each member owns complete `unselected`, `selected`, and `unavailable` State Sets. Friends mode must expose the unavailable member state without silently retaining Party membership or selection. Public Party state must reject inconsistent mode, membership, visible-member, selection, and availability relationships immutably. The Party action map must be a unique exact permutation of the manifest-owned control IDs and must reject duplicate, swapped, or self-inconsistent bindings.

Create Party and Party Settings remain absent because the source does not contain the required name-entry or settings Windows; the candidate must not fake or partially implement either flow. Title drag, title close, and focused Escape close are real and reversible. Two consecutive Leave activations with no inserted inter-gesture wait must commit once, reject once, and settle without wedging the Control in its pressed phase.

Prototype implementation is not production input; only the learned Behaviour Card, State Set, gestures, and deterministic asset manifest are retained. Party policy must stay behind the generic Window-state adapter seam and must not be encoded in the shared Control Runtime or Window. No provider request is permitted or required. Candidate evidence includes the strict real-Chromium Party Play Log, exact-candidate Play Logs for all eight earlier Windows after the shared Control Library change, and the complete Image-79 contract registry.
