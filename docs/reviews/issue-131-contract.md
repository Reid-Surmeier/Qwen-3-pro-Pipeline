# Issue 131 blind artifact contract

Review the exact packet candidate without relying on the builder narrative.

## Visual authority

- Image 79 is `artifacts/references/ro-desktop-b/reference-native.png`, SHA-256 `f4844fa9030b31b233f43244290f729db105f7256e0c0a6e889f0889bb88366f`.
- Status occupies `(0, 211, 484, 208)` on the 1536x1024 canvas.
- Source pixels define the expanded idle Status window, Japanese copy, values, chrome, title controls, spacing, and crop. Hover and pressed feedback are deterministic source-palette extensions.
- The prototype is disposable. Its retained authority is only `artifacts/references/ro-desktop-b/status/prototype-learning-manifest.json`: Behaviour Cards, State Sets, gestures, facts, and manifest decisions.
- No provider generation was used.

## Required observations

1. The 484x208 idle Status window reproduces the source crop exactly. The 4x reference and candidate crops have zero changed pixels.
2. Str, Agi, Vit, Dex, and Luk each accept real Activate and ContextActivate pointer paths. An accepted allocation spends the declared points, changes the attribute once, updates every derived value in the same public version, and visibly projects the complete semantic state in the source visual language.
3. The Int control and every control after point exhaustion remain unavailable. Activate and ContextActivate produce a named rejection, preserve the complete semantic state and committed pixels exactly, and still expose source-style transient pressed feedback inside the Status region.
4. Reversal refunds only allocations made after the source state, restores derived values in the same frame, and returns exactly to the prior rendered frame. Points never become negative.
5. Minimize uses a purpose-built 484x28 top window and restores the exact expanded frame. Title Drag uses at least 30 two-dimensional pointer samples. Title close and Escape close are real, reversible gestures.
6. Every Status control exposes a stable ID, complete State Set, source-owned geometry, semantic availability, and manifest-bound actions. Derived calculation remains in the Status Window State Adapter; the shared Stepper remains generic.
7. The Status Play Log binds to candidate `6efdc10bb46c81b1b4a8da0699228b812d3beddf`, exercises all 16 declared actions with 53 hash-verified frames, and has no unexercised controls, unexercised actions, console errors, or provider requests.
8. The native 218-contract Image 79 registry, 250 Python tests, 19 Node tests, compilation, diff check, frame capture, fidelity check, and real-input interaction baseline pass on the exact candidate.
9. Options, Skill Tree, Inventory, Storage, Equipment Card, and Equipment Items browser Play Logs are rerun against the same candidate after the shared Stepper Control change, and each deterministic verdict passes.
10. The assembled browser frame contains Status with the six earlier windows. No Window is cropped, partially replaced, or permanently covered by a screenshot layer.

Every finding must name the candidate SHA, expected behavior, actual behavior, evidence path, and violated clause. This review is advisory evidence only and does not approve the final eleven-window assembled desktop.
