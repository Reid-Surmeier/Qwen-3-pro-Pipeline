# Issue #136 blind-review contract — final Image 79 Assembly

Candidate scope: the complete 1536×1024 Image 79 desktop, all eleven
production Windows, all 239 source-inventory controls, and the shared browser
publication path. No paid or model-backed generation is part of this Assembly.

## Immutable authority

- Reference: `artifacts/references/ro-desktop-b/reference-native.png`
- SHA-256: `f4844fa9030b31b233f43244290f729db105f7256e0c0a6e889f0889bb88366f`
- Control inventory: `artifacts/references/ro-desktop-b/control-inventory.json`
- Production schema: `godot/data/image-79-control-spec.json`
- Assembly audit: `godot/data/image-79-assembly-manifest.json`

## Acceptance

1. Reset presents all eleven production Windows at the declared source
   positions, sizes, visibility, and stable relative stack. There are exactly
   239 unique source-control audit entries and no prototype owner.
2. Pointer-down raises the active Window. Title drag and supported resize
   gestures remain continuous and viewport-clamped; reload restores the reset
   composition.
3. Every declared Window and Control action remains covered by an exact-SHA
   browser Play Log. Inventory/equipment transfer and all OpenWindow routes
   pass both accepted and rejected paths in the engine contracts.
4. All eleven Windows sleep while idle. Chat Room wakes only for its accepted
   three-frame delivery and sleeps again. Continuous drag retains at least 30
   real two-dimensional Godot motion samples, publishes coalesced geometry
   updates, and emits no application console errors.
5. Cold import, engine contracts, native capture/fidelity/input, repository
   baselines, responsive browser mapping, direct-input latency, and the
   continuous-drag performance regression pass on the exact candidate.
6. Provider/model requests and additions to the generation ledger are zero.

## Required evidence

- Complete reset desktop plus moved/raised and restored browser frames.
- All eleven exact-candidate Play Logs, machine verdicts, evidence manifests,
  and representative 4× State Sets.
- Assembly audit, native QA report, browser performance report, repository
  baseline logs, and exact launch/reset instructions.
- Independent primary Playtest verdict and blind artifact verdict. A completed
  secondary FAIL blocks release; an unavailable secondary is recorded as a
  coverage limitation and does not replace the primary verdict.
