# Issue 132 Basic Info review contract

Candidate review must verify the hash-locked image-79 crop at `[0, 0, 656, 286]`, four read-only source meters, ten exact display facts, five live destination routes, and three visibly unavailable destinations. A live destination opens or raises only its declared production Window without changing target position or semantic state. Map, Chat, and Friend reject with a named error until their complete Windows exist.

Basic Info is the reset composition back layer. Status and Skill Tree retain their source overlap above it. Title drag, 656x48 purpose-built minimize/restore, title close, and focused Escape close remain reversible and preserve Meter facts and position. Prototype implementation is not production input; only the learning manifest, Behaviour Cards, State Sets, gestures, and deterministic asset manifest are retained. No provider request is permitted or required.
