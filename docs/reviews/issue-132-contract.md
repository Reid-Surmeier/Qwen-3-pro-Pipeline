# Issue 132 Basic Info review contract

Candidate review must verify SHA-256 `f4844fa9030b31b233f43244290f729db105f7256e0c0a6e889f0889bb88366f` and its image-79 crop at `[0, 0, 484, 205]`, four read-only source meters, ten exact display facts, five live destination routes, and three visibly unavailable destinations. Every destination routes through the Desktop Action Router. A live destination opens or raises only its declared production Window without changing target position or semantic state. Map, Chat, and Friend receive the same `OpenWindow` route and publish its named `ActionRoutingError` until their complete Windows exist.

Basic Info is the reset composition back layer. Status and Skill Tree retain their source overlap above it. Title drag, 484x28 purpose-built source-pixel Assembly minimize/restore, title close, and focused Escape close remain reversible and preserve Meter facts and position. Prototype implementation is not production input; only the learning manifest, Behaviour Cards, State Sets, gestures, and deterministic asset manifest are retained. No provider request is permitted or required. Candidate evidence includes Basic Info, every earlier Window regression Play Log, and the repository baselines.
