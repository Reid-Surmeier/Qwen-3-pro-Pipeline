# Issue 129 blind artifact contract

Review the exact packet candidate without relying on the builder narrative.

## Visual authority

- Image 79 is `artifacts/references/ro-desktop-b/reference-native.png`, SHA-256 `f4844fa9030b31b233f43244290f729db105f7256e0c0a6e889f0889bb88366f`.
- The Equipment Card Window occupies `(1108, 0, 424, 290)` on the 1536x1024 canvas.
- Source-owned pixels define the expanded idle card, Mistress Card detail, scrollbar, title strip, minimize control, and close control. Hover and pressed states are deterministic source-derived extensions in the same visual language.
- Image 79 does not attest content beyond the visible card detail. Scrolling must remain explicitly unavailable and report `VisualAuthorityError`; the implementation must not invent a portrait, continuation, or de-occluded pixels.
- The prototype is disposable. Its retained authority is only `artifacts/references/ro-desktop-b/equipment-card/prototype-learning-manifest.json`: Behaviour Card, State Set, gesture, and manifest decisions.

## Required observations

1. The 424x290 idle Equipment Card remains source-coherent at 4x: Mistress Card copy, blue card field, scrollbar, chrome, and title controls are aligned and legible.
2. Detail open/close state is routed through the Desktop Action Router and reports the source-attested `mistress-card` identifier through factual QA state.
3. Wheel, arrow activation, and continuous thumb Drag are real input paths. Each fails closed with `VisualAuthorityError`, retains offset zero, and provides reversible hover or pressed feedback without fabricating continuation pixels.
4. Minimize uses the purpose-built 424x28 plate and restore returns to the exact expanded idle state; it is not a runtime crop of the expanded node.
5. Title Drag uses continuous two-dimensional pointer samples and restores exactly. Title close, Escape close, detail close, and Window visibility remain synchronized.
6. Every Equipment Card control exposes stable IDs, complete declared semantic/interaction State Sets, source-owned geometry, and manifest-bound actions.
7. The Equipment Card Play Log uses real Chromium pointer, wheel, drag, click, and keyboard input, covers every required action, binds to the exact candidate SHA, and reports zero actionable console or page errors.
8. The native 172-contract Image 79 gate and repository baseline pass on the exact candidate. Options, Skill Tree, Inventory, and Storage are rerun against the same SHA after the shared ScrollView, ControlRuntime, ControlWindow, and Desktop Action Router changes.
9. The assembled browser and native frames contain Equipment Card with Options, Skill Tree, Inventory, and Storage together. Earlier Windows remain visible and coherent; the new Window does not crop or partially replace them.
10. The evidence records zero provider requests. All new visuals are deterministic crops or source-derived interaction variants from the hash-locked Image 79 reference.

Every finding must name the candidate SHA, expected behavior, actual behavior, evidence path, and violated clause. This review is advisory evidence only and does not approve the final eleven-Window assembled desktop.
